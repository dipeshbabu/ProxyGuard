# ProxyGuard test suite

`proxyguard/` tests the public statistical API, registered-study helpers, and
the experiment drivers.

Run the complete test suite with:

```bash
pytest -q tests/proxyguard
```
